name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  windows-build-test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Restore
        run: dotnet restore .\HRCompanion.slnx
      - name: Build
        run: dotnet build .\HRCompanion.slnx -c Release --no-restore
      - name: Test
        run: dotnet test .\HRCompanion.slnx -c Release --no-build --logger "trx;LogFileName=test-results.trx"
